<% @users.each do |user| %>
<% if Friendship.where(:user_id => current_user.id, :friend_id => user.id).blank? %>
<%= user.username %> || <%= link_to "Add Friend", create_friendship_user_path(user), method: :put %>
<br>
 <% else %>
<%= user.username %> || <%= link_to "Friends already", users_friends_path %> 
<br>

<% end %>

<% end %>